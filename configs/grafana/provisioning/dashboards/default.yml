apiVersion: 1

providers:
  - name: OTel Pipeline Dashboards
    orgId: 1
    folder: OTel Pipeline
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
